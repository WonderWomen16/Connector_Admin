services:
  api:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://dineshkumarmu:root@123@host.docker.internal:5432/connector
      JWT_SECRET: replace-in-production
      ENVIRONMENT: docker
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
